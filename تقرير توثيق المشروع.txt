CESAR STORE — SYSTEM BIBLE

(نسخة احترافية قابلة لإعادة البناء 100%)

🧾 1. PROJECT IDENTITY

Name: Cesar Store
Type: Scalable E-Commerce Platform
Goal: نظام إنتاجي بدون Technical Debt + قابل للتوسع

🧠 2. CORE ARCHITECTURE RULES
2.1 Single Source of Truth
Feature	Source
Orders	DB (orders table)
Tracking	order_tracking_events
Admin	Admin API فقط (NOT DB direct)
2.2 Forbidden Patterns ❌
استخدام Service Role داخل UI
قراءة DB مباشرة من صفحات
دمج APIs متعددة لنفس الداتا
🗺️ 3. SYSTEM ARCHITECTURE (ACTUAL vs TARGET)
🔴 الوضع الحالي (Actual)
Admin Page → Supabase مباشرة (Service Role)
🟢 الوضع الصحيح (Target)
Admin UI
   ↓
Admin API
   ↓
Supabase (Service Role)
🗄️ 4. DATABASE BLUEPRINT (FULL)
4.1 orders
id uuid PK
user_id uuid FK → auth.users.id
status text CHECK
total numeric
currency text DEFAULT 'EGP'
customer_snapshot jsonb
items_snapshot jsonb
created_at timestamp DEFAULT now()
4.2 order_tracking_events
id uuid PK
order_id uuid FK → orders.id
status text
actor text CHECK ('admin','system')
created_at timestamp DEFAULT now()
4.3 carts
id uuid PK
user_id uuid FK
created_at timestamp
4.4 cart_items
id uuid PK
cart_id uuid FK
product_id uuid
quantity int
🔐 5. RLS STRATEGY
orders
SELECT → user_id = auth.uid()
INSERT → auth.uid()
UPDATE → ممنوع (admin only via service role)
🔌 6. API CONTRACT (COMPLETE)
6.1 Create Order
POST /api/orders
Flow:
جلب cart
validate
إنشاء snapshot
insert order
insert tracking event
6.2 Admin Orders
GET /api/admin/orders
GET /api/admin/orders/[id]
PATCH /api/admin/orders/[id]
6.3 Tracking Update
POST /api/admin/order-tracking
🔁 7. DATA FLOW (CRITICAL)
Cart → Order
Cart (live data)
   ↓
Validation
   ↓
Snapshot freeze
   ↓
Order created
   ↓
Tracking: requested
⚠️ RULE

snapshot = immutable
لا يتم التعديل عليه بعد الإنشاء

🔄 8. REALTIME SYSTEM (SPEC)
Channels
Customer
orders:user_id=eq.{userId}
Admin
order_tracking_events
Behavior
on event → refetchOrder()

❌ ممنوع:

patch state
optimistic updates هنا
🧾 9. INVOICE SYSTEM
Endpoint
/api/invoice/[orderId]/pdf
Rules
يعتمد على snapshot فقط
لا يعتمد على products table
🛡️ 10. ADMIN SECURITY
🔴 الوضع الحالي
cookie فقط
بدون auth حقيقي
🟢 المطلوب
ربط بـ Supabase Auth
Role = admin
middleware يتحقق من role
🧨 11. CRITICAL ISSUES (REAL)
1. Service Role Exposure

موجود فعليًا في الكود ❌

الحل:
نقله إلى API فقط
2. Missing API Layer

Admin بيعتمد على DB مباشرة

3. Missing Order Creation Flow
🧪 12. TESTING MATRIX
الحالة	لازم ينجح
Order بدون items	✅
50 item	✅
invalid data	✅
realtime update	✅
🚨 13. FAILURE STRATEGY
المشكلة	الحل
DB fail	retry + log
realtime lost	manual refetch
invalid snapshot	sanitize
📈 14. ROADMAP (REALISTIC)
Phase 1 (اللي لازم يتعمل فورًا)
❗ بناء Admin API Layer
❗ إزالة Service Role من UI
❗ بناء Create Order API
Phase 2
Realtime implementation
Order tracking UI
Phase 3
Payments
Shipping
Emails
🔒 15. DEVELOPMENT LAW (MANDATORY)

(مطابقة لقواعدك)

ملف كامل فقط
لا افتراض
لا تعديل جزئي
الحفاظ على UI
كل تغيير موثق
🎯 16. FINAL TRUTH
المشروع الحالي:

🔥 أساس قوي
❌ لكنه ليس Production Ready بعد

السبب؟

مش Bugs
لكن نقص طبقات معمارية

🧠 أهم Insight

أنت بنيت "Core System" ممتاز
لكن لسه محتاج:

👉 "Execution Layers"
⚡ الخطوة القادمة (مهمة جدًا)

دلوقتي مفيش اختيار عشوائي:

لازم نبدأ بـ:
🔴 1. بناء Admin API Layer (صح)

ثم

🔴 2. نقل كل Admin UI ليه

ثم

🔴 3. بناء Order Creation