CESAR STORE — MASTER ARCHITECTURE & COMPLETION DOCUMENT (FINAL FORM)
🧾 PROJECT IDENTITY

Project Name: Cesar Store
Type: Production-Grade E-Commerce Platform

Mission:
بناء نظام متجر إلكتروني قابل للتوسع (Scalable) وآمن (Secure) بدون أي Technical Debt، مع معمارية واضحة تمنع الأخطاء المستقبلية.

🧱 TECH STACK (LOCKED)
Frontend
Next.js 14 (App Router)
TypeScript
React (Client + Server Components Hybrid)
Tailwind CSS
Backend
Supabase
PostgreSQL
Auth
Realtime
Deployment
Vercel (Edge Runtime)
Middleware (Security Layer)
🧠 CORE ARCHITECTURE PRINCIPLES (NON-NEGOTIABLE)
1. Single Source of Truth
كل state له مصدر واحد فقط
❌ ممنوع duplication (مثلاً: order status في مكانين)
2. Separation of Concerns

فصل كامل بين:

UI Layer
Business Logic
Security Layer
3. Zero Technical Debt Policy
❌ لا يوجد حلول مؤقتة
❌ لا hacks
✔ كل شيء قابل للتوسع
4. Security First
لا يوجد bypass
كل request يتم التحقق منه
🗺️ PROJECT STRUCTURE MAP (REAL)
/app
├── /admin
│   ├── /orders
│   ├── /products
│   ├── /categories
│   └── layout.tsx
│
├── /admin-login
│
├── /orders
│   ├── page.tsx
│   └── [id]/page.tsx
│
├── /api
│   ├── /orders
│   ├── /admin
│   │   ├── /orders
│   │   ├── /login
│   │   └── /logout
│
├── /cart
├── /products
└── layout.tsx
🗄️ DATABASE ARCHITECTURE (CRITICAL — FULL DEFINITION)
🔹 users
column	type	notes
id	uuid	PK (matches auth.users)
email	text	unique
name	text	
created_at	timestamp	DEFAULT now()
🔹 orders
column	type	notes
id	uuid	PK
user_id	uuid	FK → users.id
order_number	text	unique
status	text	MUST sync with tracking
subtotal	numeric	
total	numeric	
currency	text	
customer_snapshot	jsonb	immutable
items_snapshot	jsonb	immutable
created_at	timestamp	DEFAULT now()
🔹 order_tracking_events
column	type
id	uuid
order_id	uuid FK
status	text
actor	text (admin/system)
created_at	timestamp DEFAULT now()
🔹 carts
column	type
id	uuid
user_id	uuid
created_at	timestamp
🔹 cart_items
column	type
id	uuid
cart_id	uuid
product_id	uuid
quantity	int
🔐 RLS (IMPORTANT — MISSING BEFORE)
users
user can read/update own data only
orders
user يرى طلباته فقط
admin يرى الكل
order_tracking_events
user يرى events الخاصة بطلبه فقط
🔁 ORDER STATE MACHINE (LOCKED)
FLOW
requested → confirmed → preparing → shipped → delivered
            ↘ canceled
RULES
✔ أول event = requested (إجباري)
❌ ممنوع auto-confirm
✔ فقط admin يغير الحالة
✔ كل تغيير = event جديد (append only)
🔥 CRITICAL RULE

❌ لا تعتمد على orders.status كمصدر الحقيقة
✔ المصدر الحقيقي = order_tracking_events

orders.status = derived / cached فقط

🔌 API ARCHITECTURE (STRICT CONTRACTS)
Public
GET /api/orders
returns user orders
requires auth
POST /api/orders

Creates:

order
tracking event (requested)
Admin
POST /api/admin/login
sets cookie
POST /api/admin/logout
clears cookie
GET /api/admin/orders
protected
paginated (future)
GET /api/admin/orders/tracking
timeline
🔐 ADMIN SECURITY SYSTEM (FINAL FORM)
Middleware

يحمي:

/admin/*
/api/admin/*

يسمح:

/admin-login
/api/admin/login
Session Cookie
cesar_admin_session
Validation Layer
validateAdminSession()
🔥 SECURITY GUARANTEE
لا يوجد API بدون validation
لا يوجد UI bypass
🔄 REALTIME ARCHITECTURE
Channels
User
subscribe → orders (user_id)
Order Details
subscribe → tracking_events (order_id)
Admin
subscribe → all orders
Behavior
DB change → realtime push → UI update
❌ no polling
❌ no refresh
🧨 ISSUES FIXED (ROOT CAUSE LEVEL)
Issue	Root Cause	Fix
Admin access open	no middleware	middleware added
Redirect loop	wrong route	fixed
Logout broken	router issue	window.location
Status bug	wrong initial state	requested enforced
UI desync	no realtime	realtime added
⚠️ DEFERRED ISSUES (TRACKED)
1. Pagination
لازم Server-side
current UI partially broken
2. created_at Bug

مطلوب:

DEFAULT now()
ORDER BY created_at DESC, id DESC
🧱 ARCHITECTURAL RISKS (REAL)
❗ 1. Double Source of Truth
orders.status vs events
👉 لازم تثبيت واحد فقط (events)
❗ 2. UI Editing بدون فهم

👉 أكبر سبب كسر حصل فعلاً

❗ 3. Pagination غلط
client-side = كارثة مستقبلية
❗ 4. Realtime Overload
subscriptions كثيرة بدون filtering
🚀 ROADMAP (LOCKED)
Phase 1
✅ Fix Admin Orders UI
✅ Server Pagination
Phase 2
Invoice PDF System (immutable)
Phase 3
Analytics Dashboard
Phase 4
Role-based Admin (Catalog Admin Read-only)
🔒 DEVELOPMENT RULES (ENFORCED)
🚫 ممنوع
تعديل بدون كود كامل
حذف UI
إعادة كتابة صفحة
✅ مطلوب
تحليل قبل أي تعديل
Patch فقط
الحفاظ على:
UI
Logic
Data Flow
🧠 CURRENT SYSTEM STATE (REAL)
✅ Admin Auth
Secure
Stable
Middleware enforced
✅ Orders Engine
يعمل بشكل صحيح
state machine سليم
✅ Realtime
فعال
⚠️ Admin Orders Page
فيه كسر جزئي
السبب: محاولة pagination
🎯 FINAL SYSTEM STATUS

✔ Secure
✔ Stable
✔ Scalable (Core Ready)

🚨 CRITICAL NEXT STEP
Fix:
/admin/orders
Constraints:
❌ بدون تغيير UI
❌ بدون rewrite
✔ Patch فقط
✔ Server-side pagination
🧾 FINAL NOTE (IMPORTANT)

هذا المشروع:

ليس مجرد متجر
بل نظام مصمم ليكون قابل للتوسع مثل Shopify (Mini Architecture)

أي خطأ صغير في:

state
auth
data flow

قد يؤدي لانهيار النظام بالكامل.

✅ الخلاصة

أنت حاليًا في مرحلة:

Post-Architecture Stabilization

وأخطر نقطة دلوقتي:

❗ التعديل بدون تحليل (وده حصل قبل كده وسبب كسر)